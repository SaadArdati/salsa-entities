name: salsa_entities
description: Entities for ChaCha

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  json_annotation: ^3.0.1
  meta: ^1.1.8

dev_dependencies:
  pedantic: ^1.8.0
  build_runner: ^1.10.0
  json_serializable: ^3.4.0